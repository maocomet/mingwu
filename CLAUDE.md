# DeepSeek / Claude Code 协作规则

你正在参与 Mingwu v0.1 项目开发。你可以读取当前仓库中的全部项目文件，并在当前任务范围内创建或修改代码、测试、配置示例和技术文档。请先阅读 `docs/project-plan-v0.1.md`、`第一关卡完成报告.txt` 与 `第二关卡报告.txt`，以已经确定的架构、数据模型、权限边界和连接方案为准。

## 权限边界

1. `docs/project-plan-v0.1.md` 只读。不得把任何 `- [ ]` 改成 `- [x]`，不得删除、移动、重排或改写计划任务。
2. `第一关卡完成报告.txt` 与 `第二关卡报告.txt` 只读。发现设计冲突时在汇报中指出，不要自行覆盖已经确认的决定。
3. `docs/DeepSeek开发汇报.md` 只能在文件末尾追加新的检查点，不能修改或删除旧汇报。
4. 不得读取、输出、复制或提交真实 Key、Token、密码、Tunnel 凭据、数据库密码或其他秘密。
5. 不得操作 VPS、Cloudflare、GitHub 或其他外部服务，不得部署、重启或修改任何正在运行的服务。需要外部操作时只在汇报中提出。
6. 保留工作区中已有但与你当前任务无关的修改，不得回滚、覆盖、格式化或清理其他人的文件。

## Git 规则

- 允许使用只读 Git 检查，例如查看状态、差异和历史。
- 不得执行 `git add`、`git commit`、`git push`、`git pull`、合并、变基、切换或创建分支、打标签、创建 PR。
- 不得为了得到干净工作区而回滚现有修改。
- 代码是否进入暂存区、何时提交、如何分批提交以及何时推送 GitHub，统一由小喵（Codex）检查后决定。

## 开发方式

1. 开始前读取相关计划、报告、现有代码和测试，不重复已经完成的工作。
2. 一次只处理一个边界清晰的小批次，通常包含 2～4 个紧密相关的计划候选项。
3. 先实现最小完整流程，再补测试；不要同时大范围搭建许多空目录、占位文件或只有 TODO 的接口。
4. 遵守第二关报告已经确定的模块化单体、AI Actor、AuthContext、权限、追加历史、敏感信息脱敏和数据关联规则。
5. AI 身份必须由服务端凭据解析，不能相信客户端提交的 `actor_id` 或显示名称。
6. 写操作必须考虑重复调用，不能因为重试而产生重复任务、报告或更新申请。
7. 不得把模拟结果、未运行测试、仅有接口声明或 TODO 当作已经完成。
8. 修改后运行与本批次相关的检查或测试；如果因为环境缺失不能运行，应准确记录原因，不要伪造通过结果。

## 强制检查点

完成每个小批次后必须暂停继续开发，并执行以下步骤：

1. 在 `docs/DeepSeek开发汇报.md` 文件末尾追加一份检查点汇报。
2. 在 Claude Code 对话中告诉猫猫：本批次已经暂停，请让小喵检查共享汇报和代码。
3. 等待猫猫转达小喵的审核结果后再开始下一批；未经审核不得自行连续完成整关。

以下情况即使不足 2 个任务也必须立即建立检查点：

- 数据库结构或 Migration 即将新增或修改。
- 身份认证、权限或密钥处理逻辑发生变化。
- 需要改变第二关已经确定的架构或数据模型。
- 准备进行大范围重构、删除文件或引入新的主要依赖。
- 测试失败且无法在当前小批次内安全解决。
- 发现现有文件互相矛盾或需要外部服务操作。

## 汇报格式

每次追加到 `docs/DeepSeek开发汇报.md` 的内容必须包含：

- 检查点编号与时间。
- 本批次目标。
- 候选完成的计划项目原文；只能写“候选完成”，不能自行打勾。
- 实际完成内容。
- 新增、修改和删除的文件清单。
- 关键设计决定及其依据。
- 执行过的测试或检查、命令与真实结果。
- 未完成内容、已知问题和风险。
- 是否涉及数据库、身份权限、密钥、外部服务或破坏性变化。
- 建议下一批任务。
- 明确写出“等待小喵审核”。

## 完成认定

- DeepSeek 只能提交“候选完成项”。
- 小喵会检查实际差异、代码质量、测试结果、权限边界和完成标准。
- 只有小喵验收通过后，才可以修改项目计划中的复选框。
- 只有形成边界清楚、测试通过、便于回滚的一组变更后，小喵才会决定是否提交并推送 GitHub。
