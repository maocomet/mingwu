{
  "name": "mingwu",
  "version": "0.1.0",
  "private": true,
  "description": "Mingwu v0.1 - Windows AI collaboration workbench (project tracking + study room)",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test -w @mingwu/server",
    "typecheck": "npm run typecheck -w @mingwu/contracts && npm run typecheck -w @mingwu/server"
  },
  "engines": {
    "node": ">=24"
  }
}
